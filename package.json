{
  "name": "json-server-ejemplo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "dev": "nodemon ./index.js",
    "test": "json-server --watch db.json --port 3000",
    "start": "node ./index.js",
    "solo-server": "json-server --watch db.json --port 3000"
  },
  "author": "Christian Lopez",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
